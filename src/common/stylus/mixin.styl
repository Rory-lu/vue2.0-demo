
/**
** base.styl
** 这里主要存放一些抽象出来的方法样式
 */


/**
** 通过这种方式设置border的底边框
** 下面这种方式其实跟border-bottom: 1px solid $color一样
 */
border-1px($color)
  position: relative
  &:after
    display: block
    position: absolute
    left: 0
    bottom: 0
    width: 100%
    border-top: 1px solid $color
    content: ' '
/**
**  去掉1像素
*/
border-none()
  &:after
    display: none

/**
** bg-image($url)
** 根据设备像素比不同选取不同的图片
** 因为所有的图片都切了2倍图和3倍图，
** 并且格式都为name@2x.png和name@3x.png
 */
bg-image($url)
  background-image: url($url + "@2x.png")
  @media (-webkit-min-device-pixel-ratio: 3), (min-device-pixel-ratio: 3)
    background-image url($url + "@3x.png")
